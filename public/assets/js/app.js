const root=document.documentElement;
const stored=localStorage.getItem('theme'); if(stored) root.dataset.theme=stored; document.querySelectorAll('[data-theme-toggle]').forEach(button=>button.setAttribute('aria-pressed',String(root.dataset.theme==='dark')));
document.querySelectorAll('[data-theme-toggle]').forEach(button=>button.addEventListener('click',()=>{const next=root.dataset.theme==='dark'?'light':'dark';root.dataset.theme=next;localStorage.setItem('theme',next);button.setAttribute('aria-pressed',String(next==='dark'));}));
document.querySelectorAll('[data-drawer-toggle]').forEach(button=>{const drawer=document.querySelector(button.dataset.drawerToggle);if(!drawer)return;const overlay=document.querySelector(`[data-drawer-close="${button.dataset.drawerToggle}"]`);const close=()=>{drawer.classList.remove('is-open');overlay?.classList.remove('is-open');button.setAttribute('aria-expanded','false');button.focus();};button.addEventListener('click',()=>{const open=drawer.classList.toggle('is-open');overlay?.classList.toggle('is-open',open);button.setAttribute('aria-expanded',String(open));});drawer.querySelectorAll('a').forEach(link=>link.addEventListener('click',close));overlay?.addEventListener('click',close);document.addEventListener('keydown',event=>{if(event.key==='Escape'&&drawer.classList.contains('is-open'))close();});});
document.querySelectorAll('[data-confirm]').forEach(form=>form.addEventListener('submit',event=>{if(!window.confirm(form.dataset.confirm)) event.preventDefault()}));
document.querySelectorAll('[data-password-toggle]').forEach(button=>button.addEventListener('click',()=>{const input=button.closest('.password-field')?.querySelector('[data-password]');if(!input)return;input.type=input.type==='password'?'text':'password';button.textContent=input.type==='password'?'Exibir':'Ocultar';}));
document.querySelectorAll('[data-loading-form]').forEach(form=>form.addEventListener('submit',()=>{const button=form.querySelector('button[type="submit"]');if(button){button.disabled=true;button.textContent='Entrando...';}}));
document.querySelectorAll('[data-view-toggle]').forEach(button=>button.addEventListener('click',()=>{const target=button.dataset.viewToggle;document.querySelectorAll('[data-management-view]').forEach(view=>view.hidden=view.dataset.managementView!==target);}));
document.querySelectorAll('[data-team-filter]').forEach(form=>{const team=form.querySelector('[data-team-selector]');const athletes=form.querySelector('[data-team-options]');const refresh=()=>{for(const option of athletes.options){if(!option.dataset.team)continue;option.hidden=option.dataset.team!==team.value;if(option.hidden&&option.selected)athletes.value='';}};team.addEventListener('change',refresh);refresh();});
