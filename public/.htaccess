Options -Indexes -MultiViews

RewriteEngine On
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]
RewriteRule ^ index.php [QSA,L]

<FilesMatch "^\.(env|git)|.*\.(env|log|sql|sqlite|db|bak)$">
    Require all denied
</FilesMatch>
